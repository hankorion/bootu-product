spring:
  application:
    name: durain-games
  cloud:
    config:
      discovery:
        enabled: true
        service-id: BOOTU-CONFIG
      profile: dev
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8861/eureka/